# Neon Postgres
DATABASE_URL=

# Optional: comma-separated list of allowed admin origins for POST guards.
# Leave unset to default to same-origin only.
# ADMIN_ALLOWED_ORIGINS=https://admin.mubendecountryresort.com
